<Window x:Class="Client.Views.ShipFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add New Ship" Height="400" Width="400"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Name:" Grid.Row="0" Grid.Column="0" Margin="0,0,5,5" VerticalAlignment="Center" />
        <TextBox Text="{Binding Name}" Grid.Row="0" Grid.Column="1" Margin="0,0,0,5" />

        <TextBlock Text="Capacity:" Grid.Row="1" Grid.Column="0" Margin="0,0,5,5" VerticalAlignment="Center" />
        <TextBox Text="{Binding Capacity}" Grid.Row="1" Grid.Column="1" Margin="0,0,0,5" />

        <TextBlock Text="Type:" Grid.Row="2" Grid.Column="0" Margin="0,0,5,5" VerticalAlignment="Center" />
        <ComboBox ItemsSource="{Binding ShipTypeValues}"
                  SelectedItem="{Binding Type}"
                  Grid.Row="2" Grid.Column="1" Margin="0,0,0,5" />

        <TextBlock Text="Length (m):" Grid.Row="3" Grid.Column="0" Margin="0,0,5,5" VerticalAlignment="Center" />
        <TextBox Text="{Binding LengthInMeters}" Grid.Row="3" Grid.Column="1" Margin="0,0,0,5" />

        <TextBlock Text="Draft (m):" Grid.Row="4" Grid.Column="0" Margin="0,0,5,5" VerticalAlignment="Center" />
        <TextBox Text="{Binding DraftInMeters}" Grid.Row="4" Grid.Column="1" Margin="0,0,0,5" />

        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="80" Margin="5" IsDefault="True" Click="SaveButton_Click" />
            <Button Content="Cancel" Width="80" Margin="5" IsCancel="True" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>